version: '3.8'

services:
  frontend:
    image: ${DOCKERHUB_USERNAME:-sumit67hatekar}/resumemaker-frontend:latest
    container_name: resumemaker-frontend
    ports:
      - "4040:80"
    restart: always

